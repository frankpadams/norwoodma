const menu=document.querySelector('#menu'),nav=document.querySelector('#nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));
const search=document.querySelector('#resourceSearch');search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();document.querySelectorAll('#resourceGrid a').forEach(a=>{a.hidden=!!q&&!((a.textContent+' '+a.dataset.tags).toLowerCase().includes(q))})});
// Production weather adapter target: api.weather.gov. Kept graceful here so the page remains usable when opened directly from disk.
async function weather(){try{const p=await fetch('https://api.weather.gov/points/42.1945,-71.1995');if(!p.ok)throw 0;const j=await p.json();const f=await fetch(j.properties.forecast);const fj=await f.json();const now=fj.properties.periods[0];document.querySelector('#temp').textContent=`${now.temperature}° · ${now.shortForecast}`;document.querySelector('#forecast').textContent=`Norwood forecast · ${now.name}`;}catch(e){document.querySelector('#forecast').textContent='Forecast loads when hosted online';}}weather();
