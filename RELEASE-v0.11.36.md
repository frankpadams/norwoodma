# Norwood.ma v0.11.37

- Reduced the approved masthead logo to approximately 230px on wide desktop, with smaller responsive sizes.
- Reserved a fixed compact logo column so the primary navigation has substantially more room.
- Prevented desktop navigation labels from wrapping onto multiple lines.
- Preserved the hamburger navigation at narrower widths.
- Updated stylesheet cache-busting references site-wide so the latest CSS is actually loaded instead of a cached v0.11.33 stylesheet.
