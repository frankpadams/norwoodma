# Norwood.ma v0.11.28

- Reworked responsive navigation so the full top navigation collapses into a hamburger menu at widths up to 1100px.
- The hamburger changes to a close control while open.
- Mobile/tablet navigation opens as a compact dropdown below the sticky header instead of permanently occupying screen space.
- The menu closes after selecting a destination, tapping outside it, pressing Escape, or returning to desktop width.
- Desktop navigation remains visible above 1100px.
- Added cache-busting version parameters for the updated navigation assets.
